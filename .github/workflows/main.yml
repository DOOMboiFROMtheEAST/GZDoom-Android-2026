name: Build GZDoom Android 2026

on:
  push:
    branches: [ "main", "master" ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout Code with Submodules
      uses: actions/checkout@v4
      with:
        submodules: 'recursive' # Vital for pulling the native C++ doom engine submodules

    - name: Set up JDK 17
      uses: actions/setup-java@v4
      with:
        java-version: '17'
        distribution: 'temurin'
        cache: gradle

    - name: Setup Android NDK
      uses: android-actions/setup-android@v3
      # This initializes the environment required to compile the C/C++ engine files

    - name: Grant Execute Permission for Gradle
      run: chmod +x gradlew

    - name: Build GZDoom APK
      run: ./gradlew assembleDebug

    - name: Upload GZDoom APK
      uses: actions/upload-artifact@v4
      with:
        name: GZDoom-2026-Debug-APK
        path: |
          **/build/outputs/apk/debug/*.apk
          **/build/outputs/apk/release/*.apk
          
